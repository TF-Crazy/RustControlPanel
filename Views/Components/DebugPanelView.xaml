<UserControl x:Class="RustControlPanel.Views.Components.DebugPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1A1A1A">
    <Border BorderBrush="#252525" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="INTERNAL DEBUG" Foreground="#CE422B" VerticalAlignment="Center" Margin="15,0" FontWeight="Bold"/>

            <Button Content="×" HorizontalAlignment="Right" VerticalAlignment="Center" 
                    Click="CloseDebug_Click" Background="Transparent" Foreground="Gray" 
                    BorderThickness="0" FontSize="20" Margin="0,0,10,0" Cursor="Hand"/>

            <ListBox Grid.Row="1" ItemsSource="{Binding Entries}" Background="Transparent" BorderThickness="0">
            </ListBox>
        </Grid>
    </Border>
</UserControl>
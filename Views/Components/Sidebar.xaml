<!-- ════════════════════════════════════════════════════════════════════ -->
<!-- Sidebar.xaml - Modern collapsible sidebar with clean icons          -->
<!-- ════════════════════════════════════════════════════════════════════ -->

<UserControl x:Class="RustControlPanel.Views.Components.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="SidebarControl">

    <Border Background="{DynamicResource ThemeTitleBar}"
            BorderBrush="{DynamicResource ThemeCardBorder}"
            BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Toggle Button -->
            <Button Grid.Row="0"
                    Click="OnToggleClick"
                    Height="40"
                    Margin="8"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="8">
                                        <Path Data="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"
                                              Fill="{DynamicResource ThemeTextMuted}"
                                              Stretch="Uniform"
                                              Width="16" Height="16"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#20FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Navigation Items -->
            <StackPanel Grid.Row="1" Margin="8,0,8,12">
                
                <!-- Map -->
                <Border x:Name="MapBorder"
                        Height="48"
                        Margin="0,0,0,4"
                        Background="Transparent"
                        CornerRadius="6"
                        Cursor="Hand"
                        MouseLeftButtonDown="OnMapClick">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#20FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <Viewbox Width="22" Height="22" Grid.Column="0">
                            <Canvas Width="24" Height="24">
                                <Path Data="M20.5,3L20.34,3.03L15,5.1L9,3L3.36,4.9C3.15,4.97 3,5.15 3,5.38V20.5A0.5,0.5 0 0,0 3.5,21L3.66,20.97L9,18.9L15,21L20.64,19.1C20.85,19.03 21,18.85 21,18.62V3.5A0.5,0.5 0 0,0 20.5,3Z"
                                      Fill="{DynamicResource ThemeAccent}"
                                      Opacity="0.9"/>
                            </Canvas>
                        </Viewbox>
                        
                        <!-- Text -->
                        <TextBlock x:Name="MapText"
                                   Grid.Column="1"
                                   Text="Map"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource ThemeText}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Stats -->
                <Border x:Name="StatsBorder"
                        Height="48"
                        Margin="0,0,0,4"
                        Background="Transparent"
                        CornerRadius="6"
                        Cursor="Hand"
                        MouseLeftButtonDown="OnStatsClick">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#20FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Viewbox Width="22" Height="22" Grid.Column="0">
                            <Canvas Width="24" Height="24">
                                <Path Data="M16,11.78L20.24,4.45L21.97,5.45L16.74,14.5L10.23,10.75L5.46,19H22V21H2V3H4V17.54L9.5,8L16,11.78Z"
                                      Fill="{DynamicResource ThemeAccent}"
                                      Opacity="0.9"/>
                            </Canvas>
                        </Viewbox>
                        
                        <TextBlock x:Name="StatsText"
                                   Grid.Column="1"
                                   Text="Stats"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource ThemeText}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Players -->
                <Border x:Name="PlayersBorder"
                        Height="48"
                        Margin="0,0,0,4"
                        Background="Transparent"
                        CornerRadius="6"
                        Cursor="Hand"
                        MouseLeftButtonDown="OnPlayersClick">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#20FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Viewbox Width="22" Height="22" Grid.Column="0">
                            <Canvas Width="24" Height="24">
                                <Path Data="M16,17V19H2V17S2,13 9,13 16,17 16,17M12.5,7.5A3.5,3.5 0 0,1 9,11A3.5,3.5 0 0,1 5.5,7.5A3.5,3.5 0 0,1 9,4A3.5,3.5 0 0,1 12.5,7.5M15.94,13A5.32,5.32 0 0,1 18,17V19H22V17S22,13.37 15.94,13M15,4A3.39,3.39 0 0,0 13.07,4.59A5,5 0 0,1 13.07,10.41A3.39,3.39 0 0,0 15,11A3.5,3.5 0 0,0 18.5,7.5A3.5,3.5 0 0,0 15,4Z"
                                      Fill="{DynamicResource ThemeAccent}"
                                      Opacity="0.9"/>
                            </Canvas>
                        </Viewbox>
                        
                        <TextBlock x:Name="PlayersText"
                                   Grid.Column="1"
                                   Text="Players"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource ThemeText}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Console -->
                <Border x:Name="ConsoleBorder"
                        Height="48"
                        Margin="0,0,0,4"
                        Background="Transparent"
                        CornerRadius="6"
                        Cursor="Hand"
                        MouseLeftButtonDown="OnConsoleClick">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#20FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Viewbox Width="22" Height="22" Grid.Column="0">
                            <Canvas Width="24" Height="24">
                                <Path Data="M20,19V7H4V19H20M20,3A2,2 0 0,1 22,5V19A2,2 0 0,1 20,21H4A2,2 0 0,1 2,19V5C2,3.89 2.9,3 4,3H20M13,17V15H18V17H13M9.58,13L5.57,9H8.4L11.7,12.3C12.09,12.69 12.09,13.33 11.7,13.72L8.42,17H5.59L9.58,13Z"
                                      Fill="{DynamicResource ThemeAccent}"
                                      Opacity="0.9"/>
                            </Canvas>
                        </Viewbox>
                        
                        <TextBlock x:Name="ConsoleText"
                                   Grid.Column="1"
                                   Text="Console"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource ThemeText}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </Grid>
                </Border>

            </StackPanel>

        </Grid>
    </Border>
</UserControl>

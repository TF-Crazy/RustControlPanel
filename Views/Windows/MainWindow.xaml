<!-- ════════════════════════════════════════════════════════════════════ -->
<!-- MainWindow.xaml - FINAL (sans bordure, footer, titre serveur, etc.) -->
<!-- ════════════════════════════════════════════════════════════════════ -->

<Window x:Class="RustControlPanel.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RustControlPanel.ViewModels"
        xmlns:components="clr-namespace:RustControlPanel.Views.Components"
        Title="Rust Control Panel" 
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <!-- NO BORDER (removed BorderBrush and BorderThickness) -->
    <Border Background="{DynamicResource ThemeBackground}"
            CornerRadius="8"
            Effect="{DynamicResource CardShadowOuter}">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>        <!-- TitleBar -->
                <RowDefinition Height="*"/>         <!-- Content -->
                <RowDefinition Height="28"/>        <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- TITLEBAR (with working buttons)                                 -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource ThemeTitleBar}"
                    CornerRadius="8,8,0,0"
                    BorderBrush="{DynamicResource ThemeCardBorder}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="OnTitleBarMouseDown">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Logo + Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Viewbox Width="18" Height="18" Margin="0,0,8,0">
                            <Canvas Width="32" Height="32">
                                <Path Data="M16,2 L28,9 L28,23 L16,30 L4,23 L4,9 Z"
                                      Fill="{DynamicResource ThemeAccent}"/>
                                <Path Data="M12,10 L12,22 M12,10 L17,10 Q19,10 19,12 Q19,14 17,14 L12,14 M17,14 L20,22"
                                      Stroke="White" StrokeThickness="2" StrokeLineJoin="Round"/>
                            </Canvas>
                        </Viewbox>
                        
                        <TextBlock Text="Rust Control Panel" 
                                   Foreground="{DynamicResource ThemeText}"
                                   FontSize="12"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Server name (center) -->
                    <TextBlock Grid.Column="1"
                               Text="{Binding ServerName, FallbackValue='My Rust Server'}"
                               Foreground="{DynamicResource ThemeTextMuted}"
                               FontSize="11"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Window controls (FIXED) -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="─" 
                                Click="OnMinimizeClick"
                                Style="{StaticResource TitleBarButton}"
                                Width="36" Height="32"
                                FontSize="12"
                                Foreground="{DynamicResource ThemeTextMuted}"/>
                        
                        <Button x:Name="MaximizeButton"
                                Content="☐" 
                                Click="OnMaximizeClick"
                                Style="{StaticResource TitleBarButton}"
                                Width="36" Height="32"
                                FontSize="12"
                                Foreground="{DynamicResource ThemeTextMuted}"/>
                        
                        <Button Content="✕" 
                                Click="OnCloseClick"
                                Style="{StaticResource TitleBarButton}"
                                Width="36" Height="32"
                                FontSize="14"
                                Foreground="{DynamicResource ThemeTextMuted}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- MAIN CONTENT                                                     -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>    <!-- Sidebar -->
                    <ColumnDefinition Width="*"/>       <!-- Content -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar (with text) -->
                <components:Sidebar Grid.Column="0"
                                    MapClicked="OnNavigateToMap"
                                    StatsClicked="OnNavigateToStats"
                                    PlayersClicked="OnNavigateToPlayers"
                                    ConsoleClicked="OnNavigateToConsole"/>

                <!-- Content area -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>  <!-- TopBar -->
                        <RowDefinition Height="*"/>     <!-- Page content -->
                    </Grid.RowDefinitions>

                    <!-- TopBar -->
                    <components:TopBar Grid.Row="0" DataContext="{Binding}"/>

                    <!-- Page content -->
                    <ContentControl x:Name="PageContent" 
                                    Grid.Row="1"
                                    Background="{DynamicResource ThemeBackground}"/>
                </Grid>

                <!-- ═══════════════════════════════════════════════════════════════ -->
                <!-- CONNECTION LOST OVERLAY (only if disconnected)                   -->
                <!-- ═══════════════════════════════════════════════════════════════ -->
                <Border Grid.ColumnSpan="2"
                        Background="#DD000000"
                        x:Name="ConnectionOverlay"
                        Visibility="Collapsed"
                        Panel.ZIndex="999">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="⚠️" 
                                   FontSize="48"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource ThemeAccentWarning}"
                                   Margin="0,0,0,16"/>
                        
                        <TextBlock Text="Connection Lost" 
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource ThemeText}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        
                        <TextBlock HorizontalAlignment="Center"
                                   Foreground="{DynamicResource ThemeTextMuted}"
                                   FontSize="13">
                            <Run Text="Reconnecting in"/>
                            <Run Text="{Binding ReconnectCountdown, FallbackValue=5}" 
                                 Foreground="{DynamicResource ThemeAccent}"
                                 FontWeight="SemiBold"/>
                            <Run Text="seconds..."/>
                        </TextBlock>
                        
                        <Button Content="Reconnect Now"
                                Command="{Binding ReconnectCommand}"
                                Margin="0,24,0,0"
                                Padding="16,10"
                                Background="{DynamicResource ThemeAccent}"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Cursor="Hand"
                                Effect="{DynamicResource GlassGlow}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="4"
                                                        Effect="{TemplateBinding Effect}">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                      VerticalAlignment="Center"
                                                                      Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- FOOTER (Status bar)                                              -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="2"
                    Background="{DynamicResource ThemeTitleBar}"
                    BorderBrush="{DynamicResource ThemeCardBorder}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8">
                <Grid Margin="12,0">
                    <StackPanel Orientation="Horizontal">
                        <!-- Status indicator -->
                        <Ellipse Width="8" Height="8" 
                                 Margin="0,0,6,0"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource ThemeAccentSuccess}"/>
                                    <Setter Property="Effect" Value="{DynamicResource SuccessGlow}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                            <Setter Property="Fill" Value="{DynamicResource ThemeAccentDanger}"/>
                                            <Setter Property="Effect" Value="{x:Null}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        
                        <TextBlock VerticalAlignment="Center"
                                   FontSize="10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Connected"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ThemeAccentSuccess}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                            <Setter Property="Text" Value="Disconnected"/>
                                            <Setter Property="Foreground" Value="{DynamicResource ThemeAccentDanger}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <!-- Version (right) -->
                    <TextBlock Text="v1.0.0"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               FontSize="10"
                               Foreground="{DynamicResource ThemeTextMuted}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
    
</Window>
